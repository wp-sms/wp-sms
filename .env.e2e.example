# WordPress Test Environment Configuration
# Copy this file to .env.e2e and update values as needed

# Base URL for WordPress (Docker @wordpress/env default)
WP_BASE_URL=http://localhost:8888

# WordPress admin credentials (Docker @wordpress/env defaults)
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=password

# Optional: Custom nonce (usually not needed)
# WP_NONCE=
